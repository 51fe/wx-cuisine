.divider {
  display: flex;
  align-items: center;
}

.line {
  flex: 1;
  border-bottom: 1px solid #ddd;
}

.line.fixed {
  flex: none;
  width: 60rpx;
}

.content {
  margin-left: 16rpx;
  margin-right: 16rpx;
  font-weight: 600;
}